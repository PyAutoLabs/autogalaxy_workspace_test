# Scripts to skip during automated runs (smoke tests, pre-release checks, CI).
# Each entry is matched against script paths:
#   - Entries with '/' do a substring match against the file path
#   - Entries without '/' match the file stem exactly
# Add an inline # comment to document the reason for skipping.
#
# SLOW-skip convention:
#   Entries tagged `# SLOW <YYYY-MM-DD> - <reason>` mark scripts that are
#   skipped because they exceed the per-script timeout cap. These are
#   NOT permanent skips — every mega-run surfaces them with a loud warning
#   banner. Fix the performance issue and remove the SLOW marker.
#
# NEEDS_FIX convention:
#   Entries tagged `# NEEDS_FIX <YYYY-MM-DD> - <reason>` mark scripts that
#   are broken and parked as a to-do list. Like SLOW-skips, these are NOT
#   permanent skips — every mega-run surfaces them with a loud warning
#   banner. Investigate the failure, fix the underlying bug, and remove
#   the NEEDS_FIX marker.

# The 8-entry PyAutoHeart#74 "flakes at the 1800s cap" SLOW family was retimed on
# 2026-08-24 (5 repeats per Python leg, 300s cap; run 32741366534, phase-2 run
# 32664679042): every entry completed 5/5 on both legs, slowest 3-14% of the cap.
# Marker refuted — entries removed, scripts back in mega-run coverage (autolens_workspace_test#274).

# BOOTSTRAP-TARGET simulators (NOT to-do markers — correct as-is). These scripts
# write dataset/ fixtures that are no longer committed (git rm'd, gitignored); the
# consuming scripts provision them on demand via the should_simulate()/`.exists()`
# bootstrap (subprocess into these). They are invoked *through* their consumers,
# never as standalone validated test entries — running them standalone in a
# mega-run would only re-emit runtime-generated data. Skip the standalone run.
- imaging/jax_likelihood/simulator.py # BOOTSTRAP-TARGET - produces dataset/imaging/jax_test (imaging consumers + ellipse/visualization)
- interferometer/jax_likelihood/simulator.py # BOOTSTRAP-TARGET - produces dataset/interferometer/jax_test (interferometer consumers)
- ellipse/jax_likelihood/simulator.py # BOOTSTRAP-TARGET - produces dataset/ellipse/jax_test (ellipse/jax_likelihood consumers)
- multi_dataset/jax_likelihood/simulator.py # BOOTSTRAP-TARGET - produces dataset/multi_dataset/jax_test (multi consumers)
- multi_dataset/jax_likelihood/rectangular.py # NEEDS_FIX 2026-08-01 - hung to the 1800s release cap in the 2026-08-01 20:23 release-integrate run (passes ~19s otherwise; intermittent XLA compile stall, same family as autolens_workspace_test delaunay); quarantined to unblock the 2026-08-01 release; see autolens_workspace_test#245
- imaging/jax_likelihood/mge_group.py # NEEDS_FIX 2026-08-23 - hung to the 300s smoke cap in the #109 run (last output "JAX jit compiling vectorized (vmap) likelihood function", then silence for the full 300s; its sibling imaging/jax_likelihood/mge.py passes in 9.4s with a 2.5s vmap+JIT, and this script passes on main); intermittent XLA compile stall, same family as multi_dataset/jax_likelihood/rectangular.py above; quarantined to unblock the per-script timeout backport; see autolens_workspace_test#245
- imaging/jax_likelihood/rectangular_mge.py # NEEDS_FIX 2026-08-23 - hung to the 300s smoke cap on the 3.12 leg of #109 while PASSING on 3.13 for the identical commit, so the stall is a per-compile probability rather than a property of the script or the Python version; same composite-MGE family as imaging/jax_likelihood/mge_group.py above (plain mge.py passes in 9.4s); quarantined to unblock the per-script timeout backport; see autolens_workspace_test#245
